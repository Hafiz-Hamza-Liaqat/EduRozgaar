# Archive

Historical verification scripts, one-off migration helpers, L.2.6.5 ops aids, and legacy QA assets.

**Do not** add these to the active RC gate. Prefer live `npm run verify:*` scripts at the repository root.

Recovered paths remain runnable from their new locations if needed for forensic debugging.
